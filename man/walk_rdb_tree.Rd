% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/walk_rdb_tree.R
\name{walk_rdb_tree}
\alias{walk_rdb_tree}
\title{Walk the compiled Rd_db tree of a help topic}
\usage{
walk_rdb_tree(topic, package = "gsocproposal")
}
\arguments{
\item{topic}{Help topic to inspect, given \strong{unquoted}
(e.g. \code{walk_rdb_tree(sexpr_render)}) or as a string
(\code{walk_rdb_tree("sexpr_render")}). A trailing \code{.Rd} is optional.}

\item{package}{Installed package to read from. Defaults to \code{"gsocproposal"}.}
}
\value{
Invisibly, the \code{Rd} object for \code{topic}; called for the side effect of
printing the tree.
}
\description{
Pretty-prints the parse tree of a help topic as stored in the \emph{installed}
\code{.rdb} database (via \code{\link[tools]{Rd_db}}), one node per line,
indented by depth. Unlike walking the source \code{.Rd} with
\code{\link[tools]{parse_Rd}}, this shows the post-install tree: build- and
install-stage \code{Sexpr} are baked to plain text, \verb{#ifdef} blocks are
resolved, render-stage \code{Sexpr} stay live (with their \code{Rd_option}), and
user macros keep a \code{USERMACRO} trace.
}
\examples{
walk_rdb_tree(sexpr_render)
walk_rdb_tree(ifdef)
}
